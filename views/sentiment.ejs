<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/secondary.css">
    <link rel="icon" href="/images/WithoutTextLogo.png">
    <title>Sentiment Analysis</title>
</head>
<body>
    <div class="body">
        <%- include("partials/header.ejs"); %>
        <div class="senti-main">
            <h1>Find Sentiment</h1>
            <div class="senti-req">
                <h2 id="submitWord">Submit Your Word</h2>
                <form action="/sentiment" method="post" class="senti-form">
                    <textarea name="userInput" id="word-textarea" placeholder="Enter your word here..."></textarea>
                    <button class="find-button" type="submit">Submit</button>
                </form>
            </div>
            <div class="senti-res">
                <% if(locals.wrd){ %>
                    <h2> <%= wrd %> </h2>
                <% } %>
               <% if(locals.senti){ %>
                    <h2>Sentiment:</h2>
                    <p> <%= senti %> </p>
                <% } %>
            </div>
        </div>
        <%- include("partials/footer.ejs"); %>
    </div>
</body>
</html>